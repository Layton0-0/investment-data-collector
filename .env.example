# =============================================================================
# investment-data-collector 환경 변수
# =============================================================================
# 값은 investment-backend/.env 에서 복사. .env 파일은 저장소에 커밋하지 않음.
# 로컬 실행 시: cp .env.example app/.env 후 investment-backend/.env 에서 DART_API_KEY, SEC_API_KEY, DATA_COLLECTION_INTERNAL_KEY 등 복사
# Docker: docker-compose에서 investment-backend/.env 또는 인프라 .env 의 변수 주입
# =============================================================================

# ----- Spring 연동 (수집 데이터 전송) -----
SPRING_BASE_URL=http://localhost:8080
DATA_COLLECTION_INTERNAL_KEY=

# ----- Open DART (금융감독원 공시) — backend .env 와 동일 키 사용 -----
DART_API_KEY=
DART_BASE_URL=https://opendart.fss.or.kr/api
DART_COLLECT_DAYS=3

# ----- SEC EDGAR — backend .env 와 동일 키 사용 -----
SEC_API_KEY=
SEC_BASE_URL=https://data.sec.gov
SEC_COLLECT_DAYS=3
# SEC_CIKS= (선택, 쉼표 구분 CIK 목록)

# ----- 스케줄 (1 이면 기동 시 DART/SEC 주기 수집) -----
SCHEDULE_DART_SEC=0
SCHEDULE_SPEED_BUZZ=0
